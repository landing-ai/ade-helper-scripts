# Core dependencies
requests>=2.31.0
pandas>=2.1.0
pathlib2>=2.3.7

# HTML parsing & regex (standard lib covers most, but lxml can help with filings)
lxml>=4.9.0
beautifulsoup4>=4.12.0

# PDF generation
weasyprint>=62.0

# Selenium for browser automation
selenium>=4.19.0
webdriver-manager>=4.0.0

# Optional: logging/CLI helpers (already in stdlib, not required in pip)
# argparse, logging, pathlib, threading, re, json, time, os → stdlib only
